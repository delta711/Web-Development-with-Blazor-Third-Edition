@page "/parentcounter"

<CounterWithParameterAndEvent 
    IncrementAmount="@incrementamount" 
    @bind-CurrentCount="currentcount"
    @bind-CurrentCount:event="CurrentCountChanged" />

The current count is: @currentcount
@code {
    int incrementamount = 10;
    int currentcount = 0;

   

}
